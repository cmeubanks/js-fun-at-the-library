function createLibrary(libraryName) {
    var object = {
      name: libraryName,
//  { shelves:

//}
//was trying to see if I could create a shelves key within the object name
//keep getting an error that it is expecting undefined to be an object
}
return(object)
    }




module.exports = {
  createLibrary,
  // addBook,
  // checkoutBook
};
